import {AppCarousel} from './app-carousel';

describe('AppCarousel', () => {
    it('should create an instance', () => {
        const directive = new AppCarousel();
        expect(directive).toBeTruthy();
    });
});
