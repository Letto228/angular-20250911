import {ScrollWithLoading} from './scroll-with-loading';

describe('ScrollWithLoading', () => {
    it('should create an instance', () => {
        const directive = new ScrollWithLoading();
        expect(directive).toBeTruthy();
    });
});
